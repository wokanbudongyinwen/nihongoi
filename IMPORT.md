# 计划词表导入格式说明

导入 JSON 用于**创建背词计划**：App 会把每行词与本地词库（约 2 万词）做匹配，命中的直接引用，未命中的新建词条，最终按文件行序生成计划词单（背词顺序）。

支持两种导入模式（预览页顶部切换）：

- **新建计划**：每次导入建一个新计划（按 JSON 里的 planName/dailyNew）
- **追加到已有计划**：适合「同一教材分课导入」——每学完一课导入一次该课的单词，全部接进同一个计划。新词接在词单末尾；已在计划中的词自动跳过（学习状态与档位保留），但例句/笔记/关联仍会去重合并。追加不改计划的参数与激活状态。

## 完整格式

```json
{
  "planName": "N5 核心动词",
  "dailyNew": 10,
  "words": [
    {
      "term": "食べる",
      "reading": "たべる",
      "romaji": "taberu",
      "meaning": "吃",
      "pos": "动词·他动词",
      "priority": 2,
      "sentences": [
        { "text": "リンゴを食べる。", "translation": "吃苹果。" }
      ],
      "notes": [
        "和 飲む 一起记"
      ],
      "relations": [
        { "term": "飲む", "reading": "のむ", "note": "同类对比" }
      ]
    }
  ]
}
```

## 字段说明

### 顶层

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| planName | string | 否 | 计划名，缺省为「导入计划」，预览页可改。**仅新建模式生效** |
| dailyNew | number | 否 | 每次学新词的批量大小，缺省 10，预览页可改。**仅新建模式生效**（追加沿用目标计划参数） |
| words | array | 是 | 词表，按行序即背词顺序 |

### 词行字段

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| term | string | **是** | 表记（食べる）。缺 term 的行直接跳过 |
| reading | string | 否 | 读音（たべる）。参与匹配，未命中新建时留空后续补 |
| romaji | string | 否 | 罗马音。仅新建词条时写入，命中旧词不生效 |
| meaning | string | 否 | 中文释义。仅新建词条时写入，**不会覆盖旧词释义** |
| pos | string | 否 | 词性（如 动词·他动词 / 名词 / 形容词）。仅新建时写入 |
| priority | number | 否 | 档位 1/2/3，缺省 3。预览页行内标签可点按调整 |
| sentences | array | 否 | 例句列表，见下 |
| notes | array | 否 | 笔记列表（字符串数组），见下 |
| relations | array | 否 | 自由关联列表，见下 |

### 例句 sentences

```json
"sentences": [
  { "text": "リンゴを食べる。", "translation": "吃苹果。" },
  { "text": "朝ごはんを食べる。", "translation": "吃早饭。" }
]
```

| 字段 | 必填 | 说明 |
|------|------|------|
| text | **是** | 日文例句，为空的条目跳过 |
| translation | 否 | 中文译文 |

### 笔记 notes

```json
"notes": [ "和 飲む 一起记", "过去式：食べた" ]
```

字符串数组，空串跳过。

### 自由关联 relations

```json
"relations": [
  { "term": "飲む", "reading": "のむ", "note": "同类对比" },
  { "term": "炊く", "reading": "たく", "note": "" }
]
```

| 字段 | 必填 | 说明 |
|------|------|------|
| term | **是** | 目标词表记，为空的条目跳过 |
| reading | 否 | 目标词读音（提高匹配精度） |
| note | 否 | 关联理由 |

## 档位 priority 说明

| 值 | 名称 | 掌握条件 |
|----|------|----------|
| 1 | ① 一次即会 | 任意一次「记得」即掌握（首考也算），永久退出复习队列 |
| 2 | ② 两次即会 | 两次「记得」即掌握（约 3 天）；中途忘会自然延后 |
| 3 | ③ 常规 | 默认。stage 到顶 + 熟练度≥80（约一周） |

## 匹配与合并规则

导入预览页会逐行显示匹配结果（已匹配 / 需选择 / 将新建），确认后才写入。

### 主词匹配（分层）

1. **term + reading 精确匹配** → 唯一命中直接引用；多条命中需在预览页点选
2. **仅 term 匹配** → 唯一命中直接引用；多条命中需点选
3. **仅 reading 匹配** → 唯一命中直接引用；多条命中需点选
4. **全部未命中** → 新建词条

### 命中旧词时（追加合并，不覆盖）

- **词条本体（term/reading/romaji/meaning/pos）保持旧词不动**，JSON 里的这些字段不会写回
- **例句**：按日文文本与旧例句比对，相同的跳过，不同的**追加**
- **笔记**：按内容比对，相同的跳过，不同的**追加**
- **自由关联**：两词之间已存在 custom 关联则跳过（关联理由也不覆盖），否则建立

### 关联目标匹配（自动，无交互）

按「term+reading 精确 → 仅 term 取首个 → 仅 reading 唯一命中」自动解析；全部未命中时**新建目标词条**（仅 term/reading，其余留空）再建立关联。同一次导入中相同目标只新建一条。

### 将新建词条时

词行字段直接写入新词条；例句/笔记/关联直接附加，无需去重。

## 最小可用示例

只有 term 也能导入（其余字段缺省）：

```json
{
  "planName": "冲刺词表",
  "dailyNew": 20,
  "words": [
    { "term": "食べる", "reading": "たべる" },
    { "term": "行く", "reading": "いく" },
    { "term": "する", "reading": "する", "priority": 1 },
    { "term": "先生", "reading": "せんせい", "priority": 2 }
  ]
}
```

## 注意事项

- **重复导入是安全的（幂等）**：同一文件导两次，例句/笔记/关联会被去重跳过，不会翻倍；计划词单内同词多行也自动去重（按首次出现位置）；追加模式下重复的词跳过且保留学习状态
- **复习是全局的**：到期复习词跨所有计划取并集；学习状态按计划隔离——只要分课追加进同一计划，进度就是连续的
- **仅 term 唯一命中时 reading 会被忽略**：如库里的 開く 是 ひらく，导入 開く/あく 会引用 ひらく 那条——预览页会显示「→ 開く（ひらく）」，导入前可核对
- **轻微写法差异会产生并存词条**：读音写法不同导致未命中时会新建词条，与旧词并存（这正是预览页逐条确认的意义）
- 追加模式下 JSON 里的 planName/dailyNew 不生效，沿用目标计划的参数（可在计划详情页改）
